# Project 01: Moroccan Grandma Task Manager

פרויקט זה פותח במסגרת קורס AI Agents בהנחיית מלכה ברוק. המערכת מציגה סוכן בינה מלאכותית המנהל משימות עבור המשתמש תוך שימוש בפרסונה ייחודית של סבתא מרוקאית.

## ארכיטקטורת הסוכן (Models & Logic)

המערכת מבוססת על הפרדה לוגית בין ניהול הנתונים לבין עיבוד השפה והפרסונה:

### 1. שכבת הסוכן (Agent Service)
ה"מוח" של המערכת, המבוסס על מודל שפה גדול.
* **Persona:** הגדרת אישיות הסבתא באמצעות הנדסת פרומפט (System Prompt) הכוללת חום, דאגה וניואנסים תרבותיים.
* **NLP Parsing:** יכולת ניתוח טקסט חופשי והמרתו למשימות מובנות הכוללות כותרת ותוכן.
* **Context Awareness:** שמירה על הקשר השיחה והגבה רלוונטית בהתאם לדחיפות המטלות.

### 2. ניהול נתונים (Task Data)
* **Task Logic:** ניהול רשימת המשימות הכולל כותרת, פירוט וסטטוס ביצוע.
* **Integration:** חיבור ישיר בין תפוקת הסוכן לבין מסד הנתונים של ה-To-Do.

---

## תצוגת מסכי המערכת

### ממשק המשתמש והסוכן
| תצוגת שיחה וניהול משימות | תיאור הפעולה |
| :--- | :--- |
| ![Grandma Interface](./screenshots/GrandmaTaskManager.png) | הוספת משימה בשפה טבעית וקבלת תגובה מהסוכן |

---

## טכנולוגיות בשימוש
* **Backend:** Python הכולל שימוש ב-`python-dotenv` לניהול סביבת עבודה.
* **AI Engine:** שילוב מודלי שפה (LLM) וניהול פרומפטים מתקדם.
* **Frontend:** React / Node.js ליצירת ממשק שיחה מודרני.
* **Version Control:** Git & GitHub.

## הוראות הרצה
1. בתיקיית `Agent`: התקנת ספריות והרצת `python main.py`.
2. הגדרת מפתח API בקובץ `.env` בתוך תיקיית ה-Agent.
3. בתיקיית ה-Client: הרצת `npm install` ולאחר מכן `npm start`.